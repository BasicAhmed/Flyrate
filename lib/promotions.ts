/** Fixed-amount discount for larger transfers. Checked against the send
 *  amount's USDT-equivalent value (works regardless of which currency the
 *  customer is actually sending). */
export const DISCOUNT_THRESHOLD_USDT = 1500;
export const DISCOUNT_AMOUNT_USDT = 15;
